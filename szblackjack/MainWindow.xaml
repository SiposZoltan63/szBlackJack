<Window x:Class="szblackjack.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:szblackjack"
        mc:Ignorable="d"
        Title="BlackJack" Height="520" Width="900">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="BlackJack (21)" FontSize="28" FontWeight="Bold"/>
        <Border Grid.Row="1" Margin="0,12,0,8" Padding="10" CornerRadius="10" BorderThickness="1" BorderBrush="#444">
            <StackPanel>
                <TextBlock Text="osztó (Dealer)" FontSize="18" FontWeight="SemiBold"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="Lapok: " FontWeight="Bold"/>
                    <ItemsControl x:Name="DealerCards">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="4,0" Padding="8" CornerRadius="8" BorderBrush="#666" BorderThickness="1">
                                    <TextBlock Text="{Binding}" FontSize="16"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
                <TextBlock x:Name="DealerScoreText" Margin="0,8,0,0" FontSize="16"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="2" Margin="0,0,0,8" Padding="10" CornerRadius="10" BorderThickness="1" BorderBrush="#444">
            <StackPanel>
                <TextBlock Text="osztó (Dealer)" FontSize="18" FontWeight="SemiBold"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <TextBlock Text="Lapok: " FontWeight="Bold"/>
                    <ItemsControl x:Name="PlayerCards">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Margin="4,0" Padding="8" CornerRadius="8" BorderBrush="#666" BorderThickness="1">
                                    <TextBlock Text="{Binding}" FontSize="16"/>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
                <TextBlock x:Name="PlayerScoreText" Margin="0,8,0,0" FontSize="16"/>
            </StackPanel>
        </Border>
        <Border Grid.Row="3" Padding="10" CornerRadius="10" BorderThickness="1" BorderBrush="#444">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
                    <Button x:Name="Btnew" Content="új játék" Width="120" Margin="0,0,10,0" Click="Btnew_Click"/>
                    <Button x:Name="BtnHit" Content="Kérek lapot (hit)" Width="160" Margin="0,0,10,0" Click="BtnHit_Click"/>
                    <Button x:Name="BtnStand" Content="Megállok (Stand)" Width="160" Click="BtnStand_Click"/>
                </StackPanel>
                <TextBlock x:Name="StatusText" Grid.Row="1" FontSize="16" FontWeight="SemiBold"/>
                <TextBlock Grid.Row="2" Margin="0,12,0,0" Foreground="#666"
                            Text="Szabály: közelíts a 21-hez,de ne lépd túl. Az osztó 17-ig húz." TextWrapping="Wrap" />
            </Grid>
        </Border>
    </Grid>
</Window>
